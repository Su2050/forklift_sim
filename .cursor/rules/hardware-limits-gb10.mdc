---
description: Hardware limits and NUM_ENVS configuration for IsaacLab on GB10
globs: ["*.sh", "*.py", "*cfg*.py"]
---
# 硬件限制与环境数量配置 (GB10 128GB 统一内存)

## 背景 (Context)
在 NVIDIA IGX Orin / GB10 (Grace Blackwell) 架构且拥有 128GB 统一内存 (Unified Memory) 的机器上，运行带有相机渲染（例如 256x256 分辨率）的 IsaacLab 强化学习训练时，内存消耗极大。

## 规则 (Rules)

- **禁止 (DO NOT)** 将 `NUM_ENVS` 设置为 `512` 或 `1024`（在 256x256 相机分辨率下）。这会导致内存占用飙升至 110GB 以上，触发系统 OOM (Out of Memory) killer，直接杀死训练进程。
- **推荐 (DO)** 将 `NUM_ENVS=256` 作为 256x256 相机渲染下的**安全上限**。此时进程占用约 51GB 内存，既能保证较大的 Batch Size 提升训练效率，又能留出充足的系统余量，确保训练稳定运行。
- 如果必须使用更大的 `NUM_ENVS`（如 1024），则**必须**大幅降低相机分辨率（例如降至 128x128 或 64x64），或者完全关闭相机渲染 (`env.use_camera=False`)。
